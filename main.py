from chapters_downloader import ChaptersDownloader
from volume_scraper import VolumeScraper
from chapter_merger import merge_pdfs
from utils import get_page_soup






if __name__ == "__main__":
    # for downloading chapters
    # cd = ChaptersDownloader('./chapters/')
    # cd.run()

    # for merging chapters into a single pdf
    # merge_pdfs('./chapters/', './test.pdf', 5)

    # for merging chapters into 6 Colossal Edition Volumes
    # v = VolumeScraper('./')
    # v.merge_volumes()
